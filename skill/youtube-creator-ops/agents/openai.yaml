interface:
  display_name: "OpenClaw YouTube Publisher"
  short_description: "Publish a YouTube Short through OpenClaw and export a reusable run report."
  default_prompt: "Use $youtube-creator-ops to publish a YouTube Short through OpenClaw and export a reusable run report."
